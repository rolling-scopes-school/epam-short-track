## Set 1

1. Explain the concept of Dependency Injection in Angular. What problems does it solve?
1. How do you create and use a service in Angular? What is the purpose of the `@Injectable` decorator?
1. Explain the difference between `providedIn: 'root'` and registering a service in a component's providers array.
1. What are the different levels of dependency injection in Angular? Explain the provider hierarchy.
1. Explain `useClass`, `useValue`, `useFactory`, and `useExisting` in the context of Angular dependency injection.
1. What is Routing in Angular and why is it important for single-page applications?
1. How do you configure basic routing in Angular using `RouterModule` and `router-outlet`?
1. How do you handle route parameters and query parameters in Angular routing?
1. What is `ActivatedRoute` and how do you use it to access route information?
1. What are Route Guards in Angular? Describe different types of guards (`canActivate`, `canDeactivate`, `canMatch`, etc.) and their use cases.
1. Explain the difference between Template-driven Forms and Reactive Forms in Angular. When should each approach be used?
1. What are `FormControl`, `FormGroup`, and `FormArray` in the context of Reactive Forms?
1. How do you implement form validation in Angular? What are the built-in validators?
1. What is the `HttpClient` in Angular and how do you use it to make API requests?
1. How do you handle HTTP responses using Observables? What are the advantages of this approach?

## Set 2

1. What is the purpose of services in Angular? How do they facilitate code reuse and separation of concerns?
1. How do functional guards work in Angular? How do they differ from class-based guards?
1. What is the inject() function and how does it enable dependency injection in functional contexts?
1. How do you implement multi-providers in Angular? When would you use them?
1. What are injection tokens and when would you use them instead of class tokens?
1. How do you implement child routes and nested routing in Angular?
1. What is lazy loading in Angular routing and what are its benefits? How does it work with standalone components?
1. What are route resolvers and how do they help with data loading before activating a route?
1. How do you implement programmatic navigation in Angular? What is the Router service?
1. What are RouterLink and RouterLinkActive directives? How do they work?
1. How do you create custom validators for Reactive Forms? Provide examples of synchronous and asynchronous validators.
1. What is the `FormBuilder` service and how does it simplify form creation?
1. How do you handle dynamic forms in Angular? How do you add or remove form controls at runtime?
1. What are HTTP Interceptors and how can they be used to handle authentication or add headers? How do functional interceptors work?
1. How do you implement error handling for HTTP requests in Angular? What is the role of `catchError` and `retry` operators?
