## Set 1

1. What is unit testing and why is it important in Angular applications?
1. What testing frameworks and tools are commonly used for testing Angular applications?
1. What is Jasmine and how does it work with Angular testing?
1. What is Karma and what is its role in Angular testing?
1. How do you run tests in an Angular application using Angular CLI?
1. What is a test suite and a test spec in Jasmine? How do you structure them?
1. What are the basic Jasmine matchers? Provide examples (`toBe`, `toEqual`, `toBeTruthy`, etc.).
1. How do you test Angular components? What is the `TestBed` and how do you use it?
1. What is the difference between `beforeEach`, `afterEach`, `beforeAll`, and `afterAll` in Jasmine?
1. How do you create a component fixture in tests? What methods does it provide?
1. How do you test component inputs and outputs?
1. How do you test user interactions and events in components?
1. What is the purpose of `ComponentFixture.detectChanges()`?
1. How do you test services in Angular? What is dependency injection in tests?
1. What is the difference between testing with `TestBed` and testing services in isolation?

## Set 2

1. How do you mock dependencies in Angular tests? What are spies and how do you use them?
1. What is the difference between `createSpy`, `createSpyObj`, and `spyOn` in Jasmine?
1. How do you test asynchronous code in Angular? Explain `fakeAsync`, `tick`, and `flush`.
1. How do you query DOM elements in Angular tests using `DebugElement` and `nativeElement`?
1. How do you test HTTP requests in Angular? What is `HttpClientTestingModule`?
1. How do you test Angular pipes? Provide examples of testing custom pipes.
1. How do you test directives in Angular?
1. What are the best practices for writing unit tests in Angular?
1. What is code coverage and how do you measure it in Angular applications?
1. What is internationalization (i18n) in Angular? Why is it important?
1. How do you mark text for translation in Angular templates using the `i18n` attribute?
1. How do you extract translation messages using Angular CLI?
1. What file formats are used for translations in Angular (XLIFF, XMB, JSON)?
1. How do you build and serve an Angular application for different locales?
1. What is a locale ID in Angular and how do you configure it in your application?
