## Set 1

1. What is Angular and what is it used for? What are its main advantages compared to other frameworks?
2. What are Components in Angular, and how they are the foundation of an application structure?
3. How do you configure a component's selector, template, and style using the `@Component` decorator?
4. What is the execution order of the component's lifecycle hooks?
5. Difference between `ngAfterViewInit` and `ngAfterContentInit`.
6. Name and explain all the Control Flow entities. Name their directive analogs.
7. What is the change detection mechanism in Angular? Difference between `ZoneChangeDetection` and `ZonelessChangeDetection`.
8. Difference between `ChangeDetectionStrategy.Default` and `ChangeDetectionStrategy.OnPush`.
9. What are Signals in Angular? How do they improve reactivity in Angular applications?
10. What is a purpose of a `computed` function in Angular?
11. What is a purpose of a `effect` function in Angular?
12. Difference between `WritableSignal` and `Signal`. What angular funtions return `WritableSignal` and what return `Signal`?
13. How do you use `@Input()` and `@Output()` decorators for component communication? How do Signal inputs (`input()`) differ from traditional inputs?
14. What is a purpose of a `linkedSignal` function in Angular?
15. What is a content projection in Angular? How do you implement it?

## Set 2

1. What are directives in Angular? What is the difference between structural and attribute directives?
2. Name all types of directives in Angular.
3. How do you create a custom directive in Angular?
4. What is a puprpose of a `host` property of directive metadata? What is a purpose of `@HostListener` and `@HostBinding` decorators?
5. How does `*` syntax work in Angular?
6. Name all built-in structural directives in Angular. Compare them with the new control flow syntax.
7. What are a host directives in Angular? How do you apply them?
8. What are Pipes in Angular and what are they used for? Give examples of built-in pipes.
9. How do you pass multiple arguments to a pipe?
10. What is the difference between pure and impure pipes? When to use pure and when to use impure pipes?
11. Explain the difference between one-way and two-way data binding. How to implement two-way binding in Angular?
12. What is `ng-container` and what is it used for?
13. What is `ng-template` and what is it used for?
14. What is a purpose of a `ngTemplateOutlet` in Angular? What is a purpose of `ngComponentOutlet`?
15. What is dynamic components? How to create them?
